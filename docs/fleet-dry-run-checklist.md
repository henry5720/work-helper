# Fleet 工作流試跑清單

> 拿一件真的待辦，照 [fleet.md](./fleet.md) 跑一輪，邊跑邊打勾。
> **這份是消耗品** —— 跑完把「改進紀錄」那節的結論寫回主文件，然後把勾清空給下一輪用。
>
> 目的不是驗證指令能不能跑（那些都測過了），是驗證**這個流程你用起來順不順、哪一步最花時間**。
>
> **對齊版本**：2026-08-17 修訂 —— 操作規則在 `fleet-recon` / `fleet-worktree` 兩支 skill，
> [fleet.md](./fleet.md) 只留設計理由，這份是打勾表。

---

## 這輪要驗的四個新機制

2026-08-17 那輪的結論已寫回主文件，**但修法本身還沒驗過**：

- **終止條件** —— 偵察會不會又「開完 issue 繼續動手」
- **守門**（`git status --short`）—— 抓不抓得到越界、會不會誤報
- **身分卡**（`CLAUDE.local.md`）—— compact 之後知不知道要重讀 issue
- **看計畫那一關** —— 有沒有擋掉東西；會不會變成新瓶頸

---

## 這輪的設定

| 欄位 | 填這裡 |
| --- | --- |
| 日期 | |
| 目標 repo | |
| 整合分支 | |
| 這輪測的 issue | `#____` |
| slug | |
| 開始時間 | |
| 結束時間 | |

> **挑哪張單**：挑「範圍小 + 會動到畫面」的。純 docs 的單雖然安全，但測不到驗收那段，
> 整條路只跑了一半。
>
> **只開 2–3 個偵察 subagent**，不要 5 個。你要測的是流程，不是吞吐量 ——
> 而且瓶頸是你審核的速度。

---

## 0. 前置（缺任何一件，後面的判斷都不算數）

這三件是 2026-08-17 修訂加的，**沒做完不要開始**：

- [ ] 目標 repo 的 `.gitignore` 有 `CLAUDE.local.md`
      （`git check-ignore -v CLAUDE.local.md` 要命中；沒加會汙染步驟 3 的守門訊號）
- [ ] `~/code/CLAUDE.md` 存在，且含：repo 配對關係、repo 外權威文件位置、跨 repo 查證鐵則
- [ ] 目標 repo 的 `CLAUDE.md` 觸發表有「**決定要開單時**（含規劃要開幾張）→ 讀 issue 規範」
      —— 措辭要涵蓋規劃階段，不能只寫「開 / 改 issue 時」

再清掉會干擾判斷的遺留：

- [ ] `herdr agent list`，確認每個 agent 都有 `name` 欄位。沒有的用 `herdr agent rename <pane-id> <slug>` 補
- [ ] 沒有兩個 agent 共用同一個 worktree（一個 worktree 一個 agent）
- [ ] `git branch -a`，確認沒有兩條 branch 在做同一件事
- [ ] 總管理 pane 那份 checkout 切到整合分支，而且 **`git status --short` 是空的**

卡住的話記這裡：

```
```

---

## 1. 分流（目標 30 秒）

- [ ] 拿一句待辦，問自己：**能不能一句話說出「改完之後畫面或行為長怎樣」？**
- [ ] 記下你的答案：能 / 不能 / 這需求不該做
- [ ] 這件事的**來源**是什麼：Slack 清單 / PM 口述 / 順手發現 / 既有 issue
- [ ] 實際花了幾秒：____

⚠️ **非 Slack 來源沒有指紋。** 如果來源不是 Slack 清單，這輪要多做一件事：
**你自己先搜過**，或在派工那句話裡指定關鍵字。記下你用了什麼關鍵字：

```
```

**這步算過的條件**：你沒有為了分流去翻 code。一翻 code 就代表判準寫得不夠好，記在下面。

改進點：

```
```

---

## 2. 派偵察（**不開 pane、不開 worktree、不先開 issue**）

- [ ] 在總管理 pane 裡叫 `fleet-recon`
- [ ] repo 外的來源路徑（PM 文件 / prototype）—— **只有這種才給**
- [ ] 記下你**有沒有**手動貼 repo 內的路徑：有 / 沒有

**這步算過的條件**：你沒有告訴它「要開幾個」或「怎麼開」。

改進點：

```
```

---

## 3. 收偵察

- [ ] 每個 subagent 都回了 **issue 編號**（不是一整篇任務單）
- [ ] 記下總管理 pane 的 context 有沒有明顯變胖：有 / 沒有

**守門（這輪的重點，不能跳）**：

- [ ] `git -C <整合分支那份 checkout> status --short`
- [ ] 輸出是空的 → **終止條件生效了**，記一筆 ✅
- [ ] 輸出不是空的 → **有人越界**。記下是哪個檔案、用 mtime 對出時間窗口：

```
越界的檔案：
時間窗口：
該窗口內開的 issue（錨點要重驗）：
```

**每張 issue 的品質**（三個都要成立）：

- [ ] body 照**目標 repo 的模板**寫，不是照這份文件發明的格式
- [ ] 有**具體檔案路徑 + 行號**，而且路徑真的存在（`git cat-file -e` 或 grep 驗過）
- [ ] 它自己標了 ✅ / ⚠️，而且**標得對**（你看完覺得那個標籤合理）
- [ ] 標 ⚠️ 的，issue comment 裡真的有推導（不只結論）

⚠️ **如果 issue body 讀起來像 spec**，就是「一張單一個問題」沒守住 —— 記下它塞了幾件事：

```
```

改進點：

```
```

---

## 4. 決策

- [ ] 一次看完 N 則 issue
- [ ] 標 ✅ 的 → `gh issue edit <n> --add-label ready-for-agent`
- [ ] 標 ⚠️ 的 → 進 `/grill-with-docs`（這輪如果沒有 ⚠️ 的，這條跳過並註明）
- [ ] **顆粒度判斷**：有沒有兩張單其實該合成一張？（合了才開 worktree —— 一個 slug 一個 worktree）
- [ ] 實際花了幾分鐘：____

**這步算過的條件**：你是靠 issue 做決定的，沒有回頭自己去 grep。

改進點：

```
```

---

## 5. 開 worktree 派工（接單）

- [ ] 叫 `fleet-worktree`（指令都在裡面）
- [ ] 裝依賴花了幾秒：____
- [ ] `herdr agent list` 看得到這個 slug，而且**有 name**
- [ ] 寫完身分卡後跑一次 `git status --short` —— **應該還是空的**（驗 gitignore 那條有效）

**這步算過的條件**：

- [ ] agent 把**計畫 comment 回 issue 然後停了**（不是直接開始改檔案）
- [ ] 你**一個字的 context 都沒有手動貼**
- [ ] 它**沒有回頭問你背景**

⚠️ 如果它問了背景，那不是 agent 的問題，是 issue 寫得不夠。**把它問的那句記下來** ——
那句話就是 issue 模板下一版要補的欄位。

它問了什麼：

```
```

---

## 5b. 看計畫（這輪新加的關卡）

- [ ] N 份計畫**一次看完**（不是被一個一個打斷）
- [ ] 每份花了幾秒／幾分：____
- [ ] 有沒有哪份看出「它理解錯了」：有 / 沒有
- [ ] 過的用 `herdr agent prompt <slug> "計畫可以，開始做"` 放行
- [ ] 不過的直接在 issue comment 裡改方向，再叫它重讀

**這步算過的條件**：

- [ ] 你沒有為了看計畫去翻 code（要翻就是計畫寫得不夠具體）
- [ ] 簡單的單真的 10 秒就放行了 —— **沒有變成新瓶頸**

⚠️ **這關要用「有沒有擋掉東西」評估。** 跑完一輪一份都沒擋到的話記下來 ——
那表示任務單已經夠好，這關可以改成只留給複雜單。

擋到了什麼：

```
```

---

## 6. worktree 裡的第一道關

- [ ] agent 自己跑過相關測試
- [ ] agent 自己跑過 lint（只餵改動的檔案）
- [ ] agent 自己跑過 typecheck
- [ ] 你人工檢查範圍：`git diff --name-only <整合分支>..HEAD`
  - [ ] 改的檔案都在 issue 講的範圍內，**沒有多出來的**

⚠️ 沒有 `.env.local` 有沒有造成任何問題？如果 agent 在這步卡住或抱怨，**記下來**，
那會推翻「bootstrap 不用複製 env」這個結論。

有沒有卡：

```
```

---

## 7. 驗身分卡（compact 之後）

只有在這輪 agent 真的被 compact 過才驗得到。**沒發生就註明「這輪沒測到」。**

- [ ] agent compact 過了
- [ ] 在 issue 上 `gh issue comment` 加一個新決定
- [ ] `herdr agent prompt <slug> "issue 有更新，回去讀 gh issue view <n> --comments"`
- [ ] 它讀的是 **issue**，不是那份過期的 `handoff.md`
- [ ] 你**沒有**重講一次背景

這輪有沒有測到：

```
```

---

## 8. merge 進整合分支 + 驗收

- [ ] `git -C <目標 repo> merge --no-ff <slug>`
- [ ] 在整合分支開 dev server（worktree 裡不開）
- [ ] 驗收：任務單上「怎樣算解完」達成了沒（有 e2e 就跑 e2e，UI/UX 才你看）

**測出問題的話走這條**（沒問題就跳過，但註明「這輪沒測到回修路徑」）：

- [ ] `herdr agent prompt <slug> "整合測試發現：<現象>"`
- [ ] 確認它**不用你重講背景**就知道在說什麼
- [ ] 改完再 merge 一次
- [ ] **沒有用 revert**

**這步算過的條件**：整條回修來回你只打了一句話。

改進點：

```
```

---

## 9. 收尾

- [ ] `gh issue close <n> --comment "..."`
- [ ] **最後才** `herdr worktree remove --workspace <id>`
- [ ] 子 branch 沒有推上 origin
- [ ] 整合分支推上去

---

## 改進紀錄（跑完填，這節才是這份文件的重點）

### 四個新機制驗證結果

| 機制 | 生效了嗎 | 證據 / 例外 |
| --- | --- | --- |
| 終止條件（開完 issue 立刻停） | | |
| 守門（`git status --short`） | | |
| 身分卡（`CLAUDE.local.md`） | | |
| 看計畫（擋掉幾份／花多久） | | |

### 哪一步最花時間

| 步驟 | 花了多久 | 是你在等 agent，還是 agent 在等你？ |
| --- | --- | --- |
| 分流 | | |
| 偵察（N 個平行 subagent） | | |
| 決策 | | |
| 看計畫 | | |
| 實作 | | |
| 驗收 | | |

> **agent 在等你的那幾格，就是下一版要動的地方。** 你在等 agent 的那幾格不用管，
> 那是多開幾個就能吸收掉的。

### 主文件要改什麼

| 主文件哪一節 | 現在寫什麼 | 實際上是怎樣 |
| --- | --- | --- |
| | | |
| | | |

### 這輪沒測到的

（例如：沒有 ⚠️ 的單所以 grill 那條沒跑到、沒 compact 所以身分卡沒驗過、驗收沒出問題所以回修路徑沒驗過）

```
```

### 下一輪要換什麼

- [ ] 多開一個偵察 subagent
- [ ] 挑一件會跨前後端的（測「subagent 查後端只帶結論回來」那條）
- [ ] 挑一件來源**不是** Slack 的（測沒有指紋時的查重）
- [ ] 試 orchestrator 工具（`~/.claude/agents/*.md` 或 `oh-my-opencode-slim`）——
      拿一張最機械的 ✅ 單做 A/B，不要整批換
- [ ] 其他：

---

## 跑完之後

1. 把「主文件要改什麼」那張表的內容寫進 [fleet.md](./fleet.md)
2. 把這份的勾清空、「這輪的設定」清空，留給下一輪
3. 表格裡的舊內容不用留 —— 要看歷史 git 裡有
