# 回報設定存在原生 item 留言串

「Bug/需求總表」沒有回報對象欄，也不能新增欄位；指派對象又可能有多人，不能拿來猜驗收時該通知誰。工具因此把每次回報設定寫成 item 留言串裡一則可見、帶結構化 block ID 的 bot 訊息，最新一則生效；任意留言或由 bot 代發的相同文字不會被當成設定。

沒有明確設定時，真人建立的列回報給 `created_by`，bot 代建的列回報給來源註記裡的發起者。來源註記遺失時寧可不 `@` 人並警告，也不從目前的指派對象推測。這保留角色差異與變更歷程，代價是解析回報對象時必須讀 item 留言串。
