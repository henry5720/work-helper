# 複製成 .env 再填。.env 已經在 .gitignore 裡，不會被 commit。

# Slack app 的 Bot User OAuth Token
# api.slack.com/apps → 你的 app → OAuth & Permissions → 最上面
SLACK_BOT_TOKEN=xoxb-

# 要讀的那張 List 的 ID
# 在 Slack 打開那張表，從網址列取（例如 Bug/需求總表）
SLACK_LIST_ID=
